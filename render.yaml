services:
  - type: web
    name: todo-backend
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGO_URI
        value: YOUR_MONGODB_CONNECTION_STRING
      - key: PORT
        value: 5000
    plan: free